# Документ синхронизации: Codex ↔ Kimi

## Текущая задача: Добавить поле `category` в коллекцию `games`

### Статус: В ПРОЦЕССЕ

---

## Что нужно сделать

### В Directus (делает Codex)
- [ ] Добавить поле `category` (type: string) в коллекцию `games`
- [ ] Дать права на чтение поля `category` для роли Administrator

### В коде (делает Kimi)
- [ ] Вернуть `category` в API запрос `/api/games`
- [ ] Обновить типы в `lib/types.ts`
- [ ] Обновить компонент `GamesAdmin` для работы с категориями

---

## Последние действия

### Kimi (2026-02-05)
- Временно убрал `category` из API из-за ошибки 403
- Поле `category` отсутствует в коллекции `games` в Directus
- Нужно добавить поле и права

### Codex (ожидается)
- Добавить поле `category` в Directus
- Настроить права доступа
- Отметить галочки выше ☑️

---

## Как сообщить о выполнении

После добавления поля в Directus, Codex должен:
1. Отметить галочки в разделе "В Directus"
2. Написать комментарий: `Поле category добавлено в Directus`

Тогда Kimi вернёт `category` в API.

---

## Проверка

После завершения обеих частей:
```bash
# Должно работать без ошибок
curl https://directus.arena-api.ru/items/games?fields=id,name,category
```
